import React from 'react';

export const Watchlist: React.FC = () => (
  <div className="text-sub text-sm">
    Watchlist — coming next. Use the Admin tab first to paste your Stockbit token and trigger the YTD backfill.
  </div>
);
