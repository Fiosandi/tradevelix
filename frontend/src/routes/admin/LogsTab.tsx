import React from 'react';

export const LogsTab: React.FC = () => (
  <div className="bg-card border border-border rounded-2xl p-5 text-sm text-sub">
    Sync logs viewer — coming next. Use the <span className="text-text font-semibold">Credentials</span> tab's
    live log for now to watch sync activity in real time.
  </div>
);
